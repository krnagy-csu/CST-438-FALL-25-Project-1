import { getDb } from "@/db/db";
import { SQLiteBindValue } from "expo-sqlite";

export default async function insertReview() {
    return 0;
} 

// review table still needs to be created!!!
// should be compound of user id and book id.